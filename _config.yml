title: FAIR Metroline
# This appears in the html browser tab for the site title (seen mostly by search engines, not users)

description: "FAIR Metroline. A step-by-step route to making data FAIR."
# Metadata description of the website

remote_theme: ELIXIR-Belgium/elixir-toolkit-theme@4.1.0

url: ""        # leave empty for previews
baseurl: ""    # leave empty for previews

exclude:
  - README.md
  - vendor
  - Gemfile

defaults:
  -
    scope:
      path: ""
      type: "pages"
    values:
      permalink: /:basename
      layout: "page"
      sidebar: main

plugins:
  - elixir-toolkit-theme-plugins
  - jekyll-redirect-from
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-github-metadata
  - jemoji
  - webrick

theme_variables:
   github_buttons:
     edit_me: false
     history: true
   headings:
     affiliation-tiles-page: Affiliations
   topnav:
     brand_logo: assets/img/media_kit/transp_main_logo_wide_text.png
